package com.czmp.collections.service;

import org.springframework.stereotype.Service;

@Service
public class AuthenticationService {
    // As of now, since there is little business logic related to authentication porcedure the whole code is written in
    // the controller.
}
